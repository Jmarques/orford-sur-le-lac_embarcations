node_modules/
# Les captures sont committées : c'est la baseline du diff visuel.
# Seuls les artefacts de comparaison (avant/différence) restent hors dépôt.
screenshots/.diff/
.claude/settings.local.json
.DS_Store
*.log
